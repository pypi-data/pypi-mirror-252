from setuptools import setup, find_packages

setup(
    name='hebill_websites_core',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # 添加其他依赖项
    ],
    python_requires='>=3.9',
)
