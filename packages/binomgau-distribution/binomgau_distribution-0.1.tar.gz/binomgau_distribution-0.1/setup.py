from setuptools import setup

setup(name='binomgau_distribution',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['binomgau_distribution'],
      author = 'Hillary Okello',
      zip_safe=False)
