apiVersion: v1
kind: Secret
metadata:
  name: komodo-runner-secret
  namespace: default
  annotations:
    kubernetes.io/service-account.name: "komodo-runner"
type: kubernetes.io/service-account-token
