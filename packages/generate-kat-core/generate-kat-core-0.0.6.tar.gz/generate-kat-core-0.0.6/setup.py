# setup.py

from setuptools import setup

setup(
    description='',
    license='',
    long_description=open('README.md', 'r').read(),
    name='Generate-KAT Core',
    packages=['Jinja2'],
    version='0.0.6'
)
