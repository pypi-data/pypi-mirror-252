from setuptools import setup, find_packages

setup(
    name='hebill_html_document',
    version='0.12',
    packages=find_packages(),
    install_requires=[
        # 添加其他依赖项
    ],
    python_requires='>=3.9',
)
