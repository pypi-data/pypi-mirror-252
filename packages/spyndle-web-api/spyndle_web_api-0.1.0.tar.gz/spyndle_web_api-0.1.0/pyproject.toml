[tool.poetry]
name = "spyndle-web-api"
version = "0.1.0"
description = "A non-destructive Web API for Spyglass"
authors = ["Jeremy Magland"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
fastapi = "^0.109.0"
uvicorn = "^0.27.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
