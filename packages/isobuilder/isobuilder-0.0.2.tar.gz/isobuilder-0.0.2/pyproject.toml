[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "isobuilder"
description = "This is a tool for building custom ISO images used for IMRS"
version = "0.0.2"
authors = [
  { name="John Bond", email="john.bond@icann.org" },
]
dependencies = [
  "ipaddr",
  "PyYAML",
]
requires-python = ">= 3.6"

[project.scripts]
dnsops-deploy = "isobuilder.cli:main"

