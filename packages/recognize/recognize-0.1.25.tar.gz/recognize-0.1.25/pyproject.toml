[tool.poetry]
name = "recognize"
version = "0.1.25"
description = "CLI for ML Pipeline"
authors = ["Oscar King <oscarking.nl@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
recognize = "recognize.main:app"

[tool.poetry.dependencies]
python = "^3.9"
typer = { extras = ["all"], version = "^0.9.0" }
httpx = "^0.24.1"
speedtest-cli = "^2.1.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
