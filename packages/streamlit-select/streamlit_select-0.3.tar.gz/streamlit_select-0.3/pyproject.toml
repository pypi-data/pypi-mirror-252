[tool.poetry]
name = "streamlit-select"
version = "0.3"
description = "A Streamlit component to select multiple images"
authors = ["ayushexel <ayush.chaurarsia@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.setuptools.packages.find]
where = "streamlit_select"


[tool.poetry.dependencies]
python = "^3.8, !=3.9.7"
streamlit = "^1.19.0"

[tool.poetry.dev-dependencies]
watchdog = "^2.1.9"
black = "^22.6.0"
isort = "^5.10.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"