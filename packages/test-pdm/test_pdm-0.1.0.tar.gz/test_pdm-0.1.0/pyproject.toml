[project]
name = "test-pdm"
version = "0.1.0"
description = "test repository add-on"
authors = [
    { name = "Jrxvx", email = "Dxsx1226dev@gmail.com" },
]
dependencies = [
    "parse>=1.20.0",
]
requires-python = ">=3.8"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true

[tool.pdm.dev-dependencies]
test = [
    "flet-fastapi>=0.19.0",
]
