from setuptools import setup

setup(name='mk_simple_distributions',
      version='0.1',
      description='Simple Gaussian and Binomial distributions',
      packages=['mk_simple_distributions'],
      zip_safe=False)
