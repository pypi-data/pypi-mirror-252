from setuptools import setup, find_packages

setup(
    name='py-riot-standard-calls',
    version='0.3',
    packages=find_packages(),
    install_requires=[
        # Dependências necessárias
    ],
)